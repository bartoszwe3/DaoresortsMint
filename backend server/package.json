{
    "name": "backend-server",
    "version": "1.0.0",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "build": "cd '../nft minting ' && REACT_APP_API_BASE='' npm run build && cd '../backend server' && rm -rf public && cp -r '../nft minting /build' public"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "ethers": "^6.7.1",
        "express": "^4.18.2",
        "multer": "^2.0.2",
        "resend": "^6.9.2"
    },
    "devDependencies": {
        "nodemon": "^3.0.1"
    }
}